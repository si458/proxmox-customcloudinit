# proxmox-customcloudinit

## Build:
1. Install [golang](https://golang.org/doc/install) 
2. `go build main.go`

## Usage:
`./main {VM ID} {YOURFILE}`

### example:
`root@proxmox:~# ./main 102 ~/userconfig.yaml `
